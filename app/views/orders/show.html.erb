<p style="color: green"><%= notice %></p>

<%= render @order %>

<div>
  <%= link_to "Edit", edit_order_path(@order), class: "btn btn-primary" %> |
  <%= button_to "Delete", @order, method: :delete, class: "btn btn-primary" %><br>
  <%= link_to "Back to orders", orders_path, class: "btn btn-primary" %>
</div>
